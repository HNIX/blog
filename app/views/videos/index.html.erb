<h1 id="page_title">Videos</h1>

<div id="posts_wrapper" class="skinny_wrapper">
	<% @videos.each do |video| %>
	  <div class="post">
	  	<p class="date"><%= video.created_at.strftime('%A, %B %d') %></p>
	  	<h2><%= link_to video.title, video %></h2>
	  	<hr>
	  </div>
	<% end %>
</div>